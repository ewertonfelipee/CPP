#include <stdio.h>

void main()
{
    char c;
    scanf("%c", &c);
    printf("ascii table decimal value: %d\n", c);
    printf("ascii table octal value: %o\n", c);
    printf("ascii table hex value: %x\n", c);
}